class Solution:#Implement pow(x,n) which calculates x raised to power n
    def myPow(self, x: float, n: int) -> float:
        return x ** n
	#this is already a built-in function, so I dont see how this is meant to be done faster unless there's a funny binary operations solution